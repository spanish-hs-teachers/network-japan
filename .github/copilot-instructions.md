# Workspace Instructions

このワークスペースでは、見た目確認や変更確認のために bundle exec jekyll build を実行しない。

- ローカルでの Jekyll ビルドや bundle exec jekyll build を使ったテストは行わない。
- ページ確認は、実際に公開ページまたはブラウザでの表示確認を前提にする。
- HTML、Markdown、CSS の変更は、静的な構造確認とコード整合性の確認を優先する。
- ビルド確認が必要そうな場合でも、まずはユーザーに確認してから実行する。